# API Configuration
API_KEY=your-secret-api-key-here

# Server Configuration
PORT=3000

# Database Configuration (for local development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=atlas_invest

# Prisma Database URL
# For local development:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/atlas_invest
# For Docker (use this in docker-compose.yml):
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/atlas_invest
